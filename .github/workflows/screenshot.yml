name: Capturar Screenshots

on:
  schedule:
    - cron: '*/2 * * * *'  # de 2 em 2 minutos
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Instalar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Instalar dependências
        run: npm install

      - name: Executar captura
        run: node screenshot.js

      - name: Fazer commit das imagens e HTML
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git pull --rebase
          git add img/ apresentacao.html || true
          git commit -m '📸 Atualizar screenshots automáticas e HTML' || echo 'Sem alterações'
          git push
